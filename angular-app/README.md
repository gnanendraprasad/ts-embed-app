# Angular Version of the TS Embed App

This folder contains an Angular application mirroring the React based TS Embed App.
It provides a simple skeleton with a `Reports` feature module as an example.

Due to environment restrictions, project dependencies are not installed automatically.
Run `npm install` inside this directory to set up the Angular dependencies when working locally.
